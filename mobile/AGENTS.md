# Expo SDK 52

This project uses **Expo SDK 52** (expo ~52.0.23, React Native 0.76.5).

Read the exact versioned docs at https://docs.expo.dev/versions/v52.0.0/ before writing any code.

Do NOT upgrade to any version that does not exist on npm. SDK 56 does not exist — never use it.
